# action arguments...
# Supported actions: apt, deb, dpkg, dpkg-force-overwrite, debug-deb, hold, purge, extract.

apt dialog toilet u-boot-tools edid-decode logrotate
apt pm-utils triggerhappy bsdmainutils
apt xterm